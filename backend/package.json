{
  "name": "google-token-backend",
  "version": "1.0.0",
  "description": "Fastify backend verifying Google ID tokens and upserting users into SQLite",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon --watch src --exec node src/app.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "dotenv": "^16.0.0",
    "fastify": "^4.0.0",
    "google-auth-library": "^8.0.0",
    "sequelize": "^6.0.0",
    "sqlite3": "^5.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.0"
  }
}
